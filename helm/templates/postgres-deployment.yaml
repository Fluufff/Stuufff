apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    {{- include "stuufff.labels" . | nindent 4 }}
    app.kubernetes.io/name: stuufff
    app.kubernetes.io/component: postgres
  name: postgres
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stuufff
      app.kubernetes.io/component: postgres
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app.kubernetes.io/name: stuufff
        app.kubernetes.io/component: postgres
    spec:
      volumes:
      - name: postgres-admin
        secret:
          secretName: postgres-admin
          defaultMode: 0440
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-data
      - name: tmp
        emptyDir: {}
      - name: run
        emptyDir: {}
      {{- toYaml .Values.podSecurityOpts.postgres | nindent 6 }}
      securityContext:
        {{- toYaml .Values.podSecurityContext.postgres | nindent 8 }}
      containers:
      - name: postgres
        image: {{ .Values.images.postgres }}
        volumeMounts:
        - name: postgres-admin
          mountPath: /etc/secrets/postgres-admin
          readOnly: true
        - name: postgres-data
          mountPath: /var/lib/postgresql
        - name: tmp
          mountPath: /tmp
        - name: run
          mountPath: /var/run/postgresql
        env:
        - name: POSTGRES_USER_FILE
          value: /etc/secrets/postgres-admin/user
        - name: POSTGRES_PASSWORD_FILE
          value: /etc/secrets/postgres-admin/pass
        - name: POSTGRES_DB
          value: stuufff 
        securityContext:
          {{- toYaml .Values.containerSecurityContext.postgres | nindent 10 }}
        resources:
          {{- include "stuufff.resources" .Values.resources.postgres | nindent 10 }}
